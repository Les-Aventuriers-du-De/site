---
type Props = {
  id?: string;
  horizontal?: boolean;
};
const { id, horizontal = false } = Astro.props;
// TODO size sm to reduce padding?
---

<menu class:list={["menu", horizontal ? "menu--horizontal" : undefined]} {id}>
  <slot />
</menu>
